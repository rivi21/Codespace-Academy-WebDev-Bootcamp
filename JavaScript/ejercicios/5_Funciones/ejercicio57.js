/*Dada un matriz cuadrada A (array de 2 dimensiones con el mismo número de columnas que de filas,
por ejemplo: 3) construir un algoritmo que permita determinar si dicha matriz es simétrica.
Se considera que una matriz es simétrica si A[i,j] = A[j,i] para todos los elementos i,jde la matriz.*/

